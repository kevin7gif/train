server:
  port: 8001
  servlet:
    context-path: /member
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/train?useSSL=false&serverTimezone=GMT%2B8&characterEncoding=utf-8
    username: train
    password: train

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
logging:
  level:
    com.kevin.train.member.mapper: trace

